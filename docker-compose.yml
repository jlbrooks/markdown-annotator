services:
  # Development service with hot-reload
  specmark-dev:
    build:
      context: .
      target: development
    container_name: specmark-dev
    ports:
      - "5173:5173"
    volumes:
      - ./src:/app/src
      - ./public:/app/public
      - ./index.html:/app/index.html
      - ./vite.config.js:/app/vite.config.js
      - ./tailwind.config.js:/app/tailwind.config.js
      - ./postcss.config.js:/app/postcss.config.js
      - ./eslint.config.js:/app/eslint.config.js
    environment:
      - NODE_ENV=development
    networks:
      - specmark-network
    profiles:
      - dev

  # Production service with nginx
  specmark-prod:
    build:
      context: .
      target: production
    container_name: specmark-prod
    ports:
      - "8080:80"
    networks:
      - specmark-network
    profiles:
      - prod

networks:
  specmark-network:
    driver: bridge
